import {
  DRAWER_CLOSE_ANIMATION_MS,
  Drawer,
  formatBytes,
  getFileExtension,
  getTranslationKey,
  prefixFileUrlWithBackendUrl,
  uploadApi,
  useUploadFilesStreamMutation,
  useUploadFromUrlsMutation,
  validateUrls
} from "./chunk-KFFIGHOH.js";
import "./chunk-ZKKX5PUT.js";
import "./chunk-7BMJOJBY.js";
import "./chunk-IYPI7SKS.js";
import "./chunk-OC2TUMRU.js";
import "./chunk-KSK22MEK.js";
import "./chunk-PUPCWWAG.js";
import {
  usePersistentState
} from "./chunk-7BGWOS6Q.js";
import "./chunk-HU7Y2JXU.js";
import "./chunk-VNY5CTSH.js";
import "./chunk-MBK4V2X7.js";
import "./chunk-5GRIGDVB.js";
import "./chunk-QMYXYV3C.js";
import "./chunk-GC2SR66E.js";
import "./chunk-ENY765ZS.js";
import "./chunk-DB4WPAHD.js";
import "./chunk-GBSGRFMR.js";
import "./chunk-K56IZ22O.js";
import "./chunk-TUVTRC7D.js";
import "./chunk-J25QKK7L.js";
import "./chunk-4UYNGHTZ.js";
import "./chunk-TKXHHSNW.js";
import "./chunk-AES2RZR3.js";
import "./chunk-BTK6P33O.js";
import "./chunk-M3LFIPVZ.js";
import {
  getDisplayName
} from "./chunk-YFV7NR6Y.js";
import "./chunk-ZUXBB6ZK.js";
import "./chunk-M57FBRZB.js";
import "./chunk-BA3F4N32.js";
import {
  Layouts,
  useElementOnScreen
} from "./chunk-SDTFCHTV.js";
import {
  useIsMobile
} from "./chunk-XKSU7ZSE.js";
import "./chunk-A56OFBJA.js";
import "./chunk-IFOFBKTA.js";
import "./chunk-D3OQUQMU.js";
import "./chunk-AE3YE3ZB.js";
import "./chunk-4UZXJMWR.js";
import "./chunk-CYIURZMR.js";
import "./chunk-M667JV2E.js";
import "./chunk-UXMPMCSQ.js";
import "./chunk-V6WPTRDK.js";
import "./chunk-LSXNW2XL.js";
import "./chunk-3GJUYW3U.js";
import "./chunk-5IP34FYF.js";
import "./chunk-L4VZK4CF.js";
import "./chunk-4R5EVFTH.js";
import "./chunk-MQJCYUPK.js";
import {
  Page
} from "./chunk-XKVOSMGO.js";
import {
  useQueryParams
} from "./chunk-HMCN7SPH.js";
import "./chunk-JDJC5XDZ.js";
import "./chunk-XFWULAUO.js";
import "./chunk-HISBX4Y6.js";
import {
  useNotification
} from "./chunk-T2T2GMX5.js";
import "./chunk-7H7FV2YD.js";
import "./chunk-VECGBKML.js";
import "./chunk-5M5MQYI4.js";
import "./chunk-QOUV5O5E.js";
import {
  useIntl
} from "./chunk-Q7O2EJYA.js";
import "./chunk-7K6WC7UG.js";
import {
  Route,
  Routes
} from "./chunk-QECYQD6B.js";
import {
  Cm,
  Fn,
  I,
  Na,
  O1,
  Om,
  Pm,
  R,
  Rm,
  T,
  Tm,
  Um,
  Vm,
  X0,
  at,
  hd,
  i0,
  n0,
  n1,
  o0,
  r0,
  s0,
  st,
  zn
} from "./chunk-YV32BZOW.js";
import {
  A3,
  D2,
  E3,
  G5,
  H3,
  I5,
  K5,
  N5,
  Nn,
  O5,
  Q5,
  W5,
  Z5,
  an,
  c5,
  e3,
  k5,
  v3
} from "./chunk-RDGLXKB6.js";
import {
  require_jsx_runtime
} from "./chunk-LMPM4PM5.js";
import {
  gt
} from "./chunk-YUJS5A4A.js";
import "./chunk-RX6IUAGR.js";
import {
  require_react_dom
} from "./chunk-AWHRF5BE.js";
import {
  require_react
} from "./chunk-D3I3COXH.js";
import "./chunk-L3QVMQVR.js";
import {
  __toESM
} from "./chunk-LK32TJAX.js";

// node_modules/@strapi/upload/dist/admin/future/App.mjs
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
var import_react8 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/AssetsPage.mjs
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
var import_react7 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-toggle-group/dist/index.mjs
var import_react3 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-context/dist/index.mjs
var React = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
function createContextScope(scopeName, createContextScopeDeps = []) {
  let defaultContexts = [];
  function createContext32(rootComponentName, defaultContext) {
    const BaseContext = React.createContext(defaultContext);
    const index = defaultContexts.length;
    defaultContexts = [...defaultContexts, defaultContext];
    const Provider = (props) => {
      var _a;
      const { scope, children, ...context } = props;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index]) || BaseContext;
      const value = React.useMemo(() => context, Object.values(context));
      return (0, import_jsx_runtime.jsx)(Context.Provider, { value, children });
    };
    Provider.displayName = rootComponentName + "Provider";
    function useContext22(consumerName, scope) {
      var _a;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index]) || BaseContext;
      const context = React.useContext(Context);
      if (context) return context;
      if (defaultContext !== void 0) return defaultContext;
      throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
    }
    return [Provider, useContext22];
  }
  const createScope = () => {
    const scopeContexts = defaultContexts.map((defaultContext) => {
      return React.createContext(defaultContext);
    });
    return function useScope(scope) {
      const contexts = (scope == null ? void 0 : scope[scopeName]) || scopeContexts;
      return React.useMemo(
        () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
        [scope, contexts]
      );
    };
  };
  createScope.scopeName = scopeName;
  return [createContext32, composeContextScopes(createScope, ...createContextScopeDeps)];
}
function composeContextScopes(...scopes) {
  const baseScope = scopes[0];
  if (scopes.length === 1) return baseScope;
  const createScope = () => {
    const scopeHooks = scopes.map((createScope2) => ({
      useScope: createScope2(),
      scopeName: createScope2.scopeName
    }));
    return function useComposedScopes(overrideScopes) {
      const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
        const scopeProps = useScope(overrideScopes);
        const currentScope = scopeProps[`__scope${scopeName}`];
        return { ...nextScopes2, ...currentScope };
      }, {});
      return React.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
    };
  };
  createScope.scopeName = baseScope.scopeName;
  return createScope;
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-primitive/dist/index.mjs
var React4 = __toESM(require_react(), 1);
var ReactDOM = __toESM(require_react_dom(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-slot/dist/index.mjs
var React3 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-compose-refs/dist/index.mjs
var React2 = __toESM(require_react(), 1);
function setRef(ref, value) {
  if (typeof ref === "function") {
    return ref(value);
  } else if (ref !== null && ref !== void 0) {
    ref.current = value;
  }
}
function composeRefs(...refs) {
  return (node) => {
    let hasCleanup = false;
    const cleanups = refs.map((ref) => {
      const cleanup = setRef(ref, node);
      if (!hasCleanup && typeof cleanup == "function") {
        hasCleanup = true;
      }
      return cleanup;
    });
    if (hasCleanup) {
      return () => {
        for (let i = 0; i < cleanups.length; i++) {
          const cleanup = cleanups[i];
          if (typeof cleanup == "function") {
            cleanup();
          } else {
            setRef(refs[i], null);
          }
        }
      };
    }
  };
}
function useComposedRefs(...refs) {
  return React2.useCallback(composeRefs(...refs), refs);
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-slot/dist/index.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
function createSlot(ownerName) {
  const SlotClone = createSlotClone(ownerName);
  const Slot2 = React3.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    const childrenArray = React3.Children.toArray(children);
    const slottable = childrenArray.find(isSlottable);
    if (slottable) {
      const newElement = slottable.props.children;
      const newChildren = childrenArray.map((child) => {
        if (child === slottable) {
          if (React3.Children.count(newElement) > 1) return React3.Children.only(null);
          return React3.isValidElement(newElement) ? newElement.props.children : null;
        } else {
          return child;
        }
      });
      return (0, import_jsx_runtime2.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children: React3.isValidElement(newElement) ? React3.cloneElement(newElement, void 0, newChildren) : null });
    }
    return (0, import_jsx_runtime2.jsx)(SlotClone, { ...slotProps, ref: forwardedRef, children });
  });
  Slot2.displayName = `${ownerName}.Slot`;
  return Slot2;
}
var Slot = createSlot("Slot");
function createSlotClone(ownerName) {
  const SlotClone = React3.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    if (React3.isValidElement(children)) {
      const childrenRef = getElementRef(children);
      const props2 = mergeProps(slotProps, children.props);
      if (children.type !== React3.Fragment) {
        props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
      }
      return React3.cloneElement(children, props2);
    }
    return React3.Children.count(children) > 1 ? React3.Children.only(null) : null;
  });
  SlotClone.displayName = `${ownerName}.SlotClone`;
  return SlotClone;
}
var SLOTTABLE_IDENTIFIER = Symbol("radix.slottable");
function createSlottable(ownerName) {
  const Slottable2 = ({ children }) => {
    return (0, import_jsx_runtime2.jsx)(import_jsx_runtime2.Fragment, { children });
  };
  Slottable2.displayName = `${ownerName}.Slottable`;
  Slottable2.__radixId = SLOTTABLE_IDENTIFIER;
  return Slottable2;
}
var Slottable = createSlottable("Slottable");
function isSlottable(child) {
  return React3.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER;
}
function mergeProps(slotProps, childProps) {
  const overrideProps = { ...childProps };
  for (const propName in childProps) {
    const slotPropValue = slotProps[propName];
    const childPropValue = childProps[propName];
    const isHandler = /^on[A-Z]/.test(propName);
    if (isHandler) {
      if (slotPropValue && childPropValue) {
        overrideProps[propName] = (...args) => {
          const result = childPropValue(...args);
          slotPropValue(...args);
          return result;
        };
      } else if (slotPropValue) {
        overrideProps[propName] = slotPropValue;
      }
    } else if (propName === "style") {
      overrideProps[propName] = { ...slotPropValue, ...childPropValue };
    } else if (propName === "className") {
      overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
    }
  }
  return { ...slotProps, ...overrideProps };
}
function getElementRef(element) {
  var _a, _b;
  let getter = (_a = Object.getOwnPropertyDescriptor(element.props, "ref")) == null ? void 0 : _a.get;
  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.ref;
  }
  getter = (_b = Object.getOwnPropertyDescriptor(element, "ref")) == null ? void 0 : _b.get;
  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.props.ref;
  }
  return element.props.ref || element.ref;
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-primitive/dist/index.mjs
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var NODES = [
  "a",
  "button",
  "div",
  "form",
  "h2",
  "h3",
  "img",
  "input",
  "label",
  "li",
  "nav",
  "ol",
  "p",
  "select",
  "span",
  "svg",
  "ul"
];
var Primitive = NODES.reduce((primitive, node) => {
  const Slot2 = createSlot(`Primitive.${node}`);
  const Node2 = React4.forwardRef((props, forwardedRef) => {
    const { asChild, ...primitiveProps } = props;
    const Comp = asChild ? Slot2 : node;
    if (typeof window !== "undefined") {
      window[Symbol.for("radix-ui")] = true;
    }
    return (0, import_jsx_runtime3.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
  });
  Node2.displayName = `Primitive.${node}`;
  return { ...primitive, [node]: Node2 };
}, {});

// node_modules/@strapi/upload/node_modules/@radix-ui/react-roving-focus/dist/index.mjs
var React13 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/primitive/dist/index.mjs
var canUseDOM = !!(typeof window !== "undefined" && window.document && window.document.createElement);
function composeEventHandlers(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
  return function handleEvent(event) {
    originalEventHandler == null ? void 0 : originalEventHandler(event);
    if (checkForDefaultPrevented === false || !event.defaultPrevented) {
      return ourEventHandler == null ? void 0 : ourEventHandler(event);
    }
  };
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-collection/dist/index.mjs
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
function createCollection(name) {
  const PROVIDER_NAME = name + "CollectionProvider";
  const [createCollectionContext, createCollectionScope2] = createContextScope(PROVIDER_NAME);
  const [CollectionProviderImpl, useCollectionContext] = createCollectionContext(
    PROVIDER_NAME,
    { collectionRef: { current: null }, itemMap: /* @__PURE__ */ new Map() }
  );
  const CollectionProvider = (props) => {
    const { scope, children } = props;
    const ref = import_react.default.useRef(null);
    const itemMap = import_react.default.useRef(/* @__PURE__ */ new Map()).current;
    return (0, import_jsx_runtime4.jsx)(CollectionProviderImpl, { scope, itemMap, collectionRef: ref, children });
  };
  CollectionProvider.displayName = PROVIDER_NAME;
  const COLLECTION_SLOT_NAME = name + "CollectionSlot";
  const CollectionSlotImpl = createSlot(COLLECTION_SLOT_NAME);
  const CollectionSlot = import_react.default.forwardRef(
    (props, forwardedRef) => {
      const { scope, children } = props;
      const context = useCollectionContext(COLLECTION_SLOT_NAME, scope);
      const composedRefs = useComposedRefs(forwardedRef, context.collectionRef);
      return (0, import_jsx_runtime4.jsx)(CollectionSlotImpl, { ref: composedRefs, children });
    }
  );
  CollectionSlot.displayName = COLLECTION_SLOT_NAME;
  const ITEM_SLOT_NAME = name + "CollectionItemSlot";
  const ITEM_DATA_ATTR = "data-radix-collection-item";
  const CollectionItemSlotImpl = createSlot(ITEM_SLOT_NAME);
  const CollectionItemSlot = import_react.default.forwardRef(
    (props, forwardedRef) => {
      const { scope, children, ...itemData } = props;
      const ref = import_react.default.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const context = useCollectionContext(ITEM_SLOT_NAME, scope);
      import_react.default.useEffect(() => {
        context.itemMap.set(ref, { ref, ...itemData });
        return () => void context.itemMap.delete(ref);
      });
      return (0, import_jsx_runtime4.jsx)(CollectionItemSlotImpl, { ...{ [ITEM_DATA_ATTR]: "" }, ref: composedRefs, children });
    }
  );
  CollectionItemSlot.displayName = ITEM_SLOT_NAME;
  function useCollection2(scope) {
    const context = useCollectionContext(name + "CollectionConsumer", scope);
    const getItems = import_react.default.useCallback(() => {
      const collectionNode = context.collectionRef.current;
      if (!collectionNode) return [];
      const orderedNodes = Array.from(collectionNode.querySelectorAll(`[${ITEM_DATA_ATTR}]`));
      const items = Array.from(context.itemMap.values());
      const orderedItems = items.sort(
        (a, b) => orderedNodes.indexOf(a.ref.current) - orderedNodes.indexOf(b.ref.current)
      );
      return orderedItems;
    }, [context.collectionRef, context.itemMap]);
    return getItems;
  }
  return [
    { Provider: CollectionProvider, Slot: CollectionSlot, ItemSlot: CollectionItemSlot },
    useCollection2,
    createCollectionScope2
  ];
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-id/dist/index.mjs
var React7 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-layout-effect/dist/index.mjs
var React6 = __toESM(require_react(), 1);
var useLayoutEffect2 = (globalThis == null ? void 0 : globalThis.document) ? React6.useLayoutEffect : () => {
};

// node_modules/@strapi/upload/node_modules/@radix-ui/react-id/dist/index.mjs
var useReactId = React7[" useId ".trim().toString()] || (() => void 0);
var count = 0;
function useId(deterministicId) {
  const [id, setId] = React7.useState(useReactId());
  useLayoutEffect2(() => {
    if (!deterministicId) setId((reactId) => reactId ?? String(count++));
  }, [deterministicId]);
  return deterministicId || (id ? `radix-${id}` : "");
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-callback-ref/dist/index.mjs
var React8 = __toESM(require_react(), 1);
function useCallbackRef(callback) {
  const callbackRef = React8.useRef(callback);
  React8.useEffect(() => {
    callbackRef.current = callback;
  });
  return React8.useMemo(() => (...args) => {
    var _a;
    return (_a = callbackRef.current) == null ? void 0 : _a.call(callbackRef, ...args);
  }, []);
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-controllable-state/dist/index.mjs
var React11 = __toESM(require_react(), 1);
var React23 = __toESM(require_react(), 1);

// node_modules/@radix-ui/react-use-effect-event/node_modules/@radix-ui/react-use-layout-effect/dist/index.mjs
var React9 = __toESM(require_react(), 1);
var useLayoutEffect22 = (globalThis == null ? void 0 : globalThis.document) ? React9.useLayoutEffect : () => {
};

// node_modules/@radix-ui/react-use-effect-event/dist/index.mjs
var React10 = __toESM(require_react(), 1);
var useReactEffectEvent = React10[" useEffectEvent ".trim().toString()];
var useReactInsertionEffect = React10[" useInsertionEffect ".trim().toString()];

// node_modules/@strapi/upload/node_modules/@radix-ui/react-use-controllable-state/dist/index.mjs
var useInsertionEffect = React11[" useInsertionEffect ".trim().toString()] || useLayoutEffect2;
function useControllableState({
  prop,
  defaultProp,
  onChange = () => {
  },
  caller
}) {
  const [uncontrolledProp, setUncontrolledProp, onChangeRef] = useUncontrolledState({
    defaultProp,
    onChange
  });
  const isControlled = prop !== void 0;
  const value = isControlled ? prop : uncontrolledProp;
  if (true) {
    const isControlledRef = React11.useRef(prop !== void 0);
    React11.useEffect(() => {
      const wasControlled = isControlledRef.current;
      if (wasControlled !== isControlled) {
        const from = wasControlled ? "controlled" : "uncontrolled";
        const to = isControlled ? "controlled" : "uncontrolled";
        console.warn(
          `${caller} is changing from ${from} to ${to}. Components should not switch from controlled to uncontrolled (or vice versa). Decide between using a controlled or uncontrolled value for the lifetime of the component.`
        );
      }
      isControlledRef.current = isControlled;
    }, [isControlled, caller]);
  }
  const setValue = React11.useCallback(
    (nextValue) => {
      var _a;
      if (isControlled) {
        const value2 = isFunction(nextValue) ? nextValue(prop) : nextValue;
        if (value2 !== prop) {
          (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value2);
        }
      } else {
        setUncontrolledProp(nextValue);
      }
    },
    [isControlled, prop, setUncontrolledProp, onChangeRef]
  );
  return [value, setValue];
}
function useUncontrolledState({
  defaultProp,
  onChange
}) {
  const [value, setValue] = React11.useState(defaultProp);
  const prevValueRef = React11.useRef(value);
  const onChangeRef = React11.useRef(onChange);
  useInsertionEffect(() => {
    onChangeRef.current = onChange;
  }, [onChange]);
  React11.useEffect(() => {
    var _a;
    if (prevValueRef.current !== value) {
      (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value);
      prevValueRef.current = value;
    }
  }, [value, prevValueRef]);
  return [value, setValue, onChangeRef];
}
function isFunction(value) {
  return typeof value === "function";
}
var SYNC_STATE = Symbol("RADIX:SYNC_STATE");

// node_modules/@strapi/upload/node_modules/@radix-ui/react-direction/dist/index.mjs
var React12 = __toESM(require_react(), 1);
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var DirectionContext = React12.createContext(void 0);
function useDirection(localDir) {
  const globalDir = React12.useContext(DirectionContext);
  return localDir || globalDir || "ltr";
}

// node_modules/@strapi/upload/node_modules/@radix-ui/react-roving-focus/dist/index.mjs
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var ENTRY_FOCUS = "rovingFocusGroup.onEntryFocus";
var EVENT_OPTIONS = { bubbles: false, cancelable: true };
var GROUP_NAME = "RovingFocusGroup";
var [Collection, useCollection, createCollectionScope] = createCollection(GROUP_NAME);
var [createRovingFocusGroupContext, createRovingFocusGroupScope] = createContextScope(
  GROUP_NAME,
  [createCollectionScope]
);
var [RovingFocusProvider, useRovingFocusContext] = createRovingFocusGroupContext(GROUP_NAME);
var RovingFocusGroup = React13.forwardRef(
  (props, forwardedRef) => {
    return (0, import_jsx_runtime7.jsx)(Collection.Provider, { scope: props.__scopeRovingFocusGroup, children: (0, import_jsx_runtime7.jsx)(Collection.Slot, { scope: props.__scopeRovingFocusGroup, children: (0, import_jsx_runtime7.jsx)(RovingFocusGroupImpl, { ...props, ref: forwardedRef }) }) });
  }
);
RovingFocusGroup.displayName = GROUP_NAME;
var RovingFocusGroupImpl = React13.forwardRef((props, forwardedRef) => {
  const {
    __scopeRovingFocusGroup,
    orientation,
    loop = false,
    dir,
    currentTabStopId: currentTabStopIdProp,
    defaultCurrentTabStopId,
    onCurrentTabStopIdChange,
    onEntryFocus,
    preventScrollOnEntryFocus = false,
    ...groupProps
  } = props;
  const ref = React13.useRef(null);
  const composedRefs = useComposedRefs(forwardedRef, ref);
  const direction = useDirection(dir);
  const [currentTabStopId, setCurrentTabStopId] = useControllableState({
    prop: currentTabStopIdProp,
    defaultProp: defaultCurrentTabStopId ?? null,
    onChange: onCurrentTabStopIdChange,
    caller: GROUP_NAME
  });
  const [isTabbingBackOut, setIsTabbingBackOut] = React13.useState(false);
  const handleEntryFocus = useCallbackRef(onEntryFocus);
  const getItems = useCollection(__scopeRovingFocusGroup);
  const isClickFocusRef = React13.useRef(false);
  const [focusableItemsCount, setFocusableItemsCount] = React13.useState(0);
  React13.useEffect(() => {
    const node = ref.current;
    if (node) {
      node.addEventListener(ENTRY_FOCUS, handleEntryFocus);
      return () => node.removeEventListener(ENTRY_FOCUS, handleEntryFocus);
    }
  }, [handleEntryFocus]);
  return (0, import_jsx_runtime7.jsx)(
    RovingFocusProvider,
    {
      scope: __scopeRovingFocusGroup,
      orientation,
      dir: direction,
      loop,
      currentTabStopId,
      onItemFocus: React13.useCallback(
        (tabStopId) => setCurrentTabStopId(tabStopId),
        [setCurrentTabStopId]
      ),
      onItemShiftTab: React13.useCallback(() => setIsTabbingBackOut(true), []),
      onFocusableItemAdd: React13.useCallback(
        () => setFocusableItemsCount((prevCount) => prevCount + 1),
        []
      ),
      onFocusableItemRemove: React13.useCallback(
        () => setFocusableItemsCount((prevCount) => prevCount - 1),
        []
      ),
      children: (0, import_jsx_runtime7.jsx)(
        Primitive.div,
        {
          tabIndex: isTabbingBackOut || focusableItemsCount === 0 ? -1 : 0,
          "data-orientation": orientation,
          ...groupProps,
          ref: composedRefs,
          style: { outline: "none", ...props.style },
          onMouseDown: composeEventHandlers(props.onMouseDown, () => {
            isClickFocusRef.current = true;
          }),
          onFocus: composeEventHandlers(props.onFocus, (event) => {
            const isKeyboardFocus = !isClickFocusRef.current;
            if (event.target === event.currentTarget && isKeyboardFocus && !isTabbingBackOut) {
              const entryFocusEvent = new CustomEvent(ENTRY_FOCUS, EVENT_OPTIONS);
              event.currentTarget.dispatchEvent(entryFocusEvent);
              if (!entryFocusEvent.defaultPrevented) {
                const items = getItems().filter((item) => item.focusable);
                const activeItem = items.find((item) => item.active);
                const currentItem = items.find((item) => item.id === currentTabStopId);
                const candidateItems = [activeItem, currentItem, ...items].filter(
                  Boolean
                );
                const candidateNodes = candidateItems.map((item) => item.ref.current);
                focusFirst(candidateNodes, preventScrollOnEntryFocus);
              }
            }
            isClickFocusRef.current = false;
          }),
          onBlur: composeEventHandlers(props.onBlur, () => setIsTabbingBackOut(false))
        }
      )
    }
  );
});
var ITEM_NAME = "RovingFocusGroupItem";
var RovingFocusGroupItem = React13.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeRovingFocusGroup,
      focusable = true,
      active = false,
      tabStopId,
      children,
      ...itemProps
    } = props;
    const autoId = useId();
    const id = tabStopId || autoId;
    const context = useRovingFocusContext(ITEM_NAME, __scopeRovingFocusGroup);
    const isCurrentTabStop = context.currentTabStopId === id;
    const getItems = useCollection(__scopeRovingFocusGroup);
    const { onFocusableItemAdd, onFocusableItemRemove, currentTabStopId } = context;
    React13.useEffect(() => {
      if (focusable) {
        onFocusableItemAdd();
        return () => onFocusableItemRemove();
      }
    }, [focusable, onFocusableItemAdd, onFocusableItemRemove]);
    return (0, import_jsx_runtime7.jsx)(
      Collection.ItemSlot,
      {
        scope: __scopeRovingFocusGroup,
        id,
        focusable,
        active,
        children: (0, import_jsx_runtime7.jsx)(
          Primitive.span,
          {
            tabIndex: isCurrentTabStop ? 0 : -1,
            "data-orientation": context.orientation,
            ...itemProps,
            ref: forwardedRef,
            onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
              if (!focusable) event.preventDefault();
              else context.onItemFocus(id);
            }),
            onFocus: composeEventHandlers(props.onFocus, () => context.onItemFocus(id)),
            onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
              if (event.key === "Tab" && event.shiftKey) {
                context.onItemShiftTab();
                return;
              }
              if (event.target !== event.currentTarget) return;
              const focusIntent = getFocusIntent(event, context.orientation, context.dir);
              if (focusIntent !== void 0) {
                if (event.metaKey || event.ctrlKey || event.altKey || event.shiftKey) return;
                event.preventDefault();
                const items = getItems().filter((item) => item.focusable);
                let candidateNodes = items.map((item) => item.ref.current);
                if (focusIntent === "last") candidateNodes.reverse();
                else if (focusIntent === "prev" || focusIntent === "next") {
                  if (focusIntent === "prev") candidateNodes.reverse();
                  const currentIndex = candidateNodes.indexOf(event.currentTarget);
                  candidateNodes = context.loop ? wrapArray(candidateNodes, currentIndex + 1) : candidateNodes.slice(currentIndex + 1);
                }
                setTimeout(() => focusFirst(candidateNodes));
              }
            }),
            children: typeof children === "function" ? children({ isCurrentTabStop, hasTabStop: currentTabStopId != null }) : children
          }
        )
      }
    );
  }
);
RovingFocusGroupItem.displayName = ITEM_NAME;
var MAP_KEY_TO_FOCUS_INTENT = {
  ArrowLeft: "prev",
  ArrowUp: "prev",
  ArrowRight: "next",
  ArrowDown: "next",
  PageUp: "first",
  Home: "first",
  PageDown: "last",
  End: "last"
};
function getDirectionAwareKey(key, dir) {
  if (dir !== "rtl") return key;
  return key === "ArrowLeft" ? "ArrowRight" : key === "ArrowRight" ? "ArrowLeft" : key;
}
function getFocusIntent(event, orientation, dir) {
  const key = getDirectionAwareKey(event.key, dir);
  if (orientation === "vertical" && ["ArrowLeft", "ArrowRight"].includes(key)) return void 0;
  if (orientation === "horizontal" && ["ArrowUp", "ArrowDown"].includes(key)) return void 0;
  return MAP_KEY_TO_FOCUS_INTENT[key];
}
function focusFirst(candidates, preventScroll = false) {
  const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
  for (const candidate of candidates) {
    if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
    candidate.focus({ preventScroll });
    if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
  }
}
function wrapArray(array, startIndex) {
  return array.map((_, index) => array[(startIndex + index) % array.length]);
}
var Root = RovingFocusGroup;
var Item = RovingFocusGroupItem;

// node_modules/@strapi/upload/node_modules/@radix-ui/react-toggle/dist/index.mjs
var React14 = __toESM(require_react(), 1);
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var NAME = "Toggle";
var Toggle = React14.forwardRef((props, forwardedRef) => {
  const { pressed: pressedProp, defaultPressed, onPressedChange, ...buttonProps } = props;
  const [pressed, setPressed] = useControllableState({
    prop: pressedProp,
    onChange: onPressedChange,
    defaultProp: defaultPressed ?? false,
    caller: NAME
  });
  return (0, import_jsx_runtime8.jsx)(
    Primitive.button,
    {
      type: "button",
      "aria-pressed": pressed,
      "data-state": pressed ? "on" : "off",
      "data-disabled": props.disabled ? "" : void 0,
      ...buttonProps,
      ref: forwardedRef,
      onClick: composeEventHandlers(props.onClick, () => {
        if (!props.disabled) {
          setPressed(!pressed);
        }
      })
    }
  );
});
Toggle.displayName = NAME;

// node_modules/@strapi/upload/node_modules/@radix-ui/react-toggle-group/dist/index.mjs
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var TOGGLE_GROUP_NAME = "ToggleGroup";
var [createToggleGroupContext, createToggleGroupScope] = createContextScope(TOGGLE_GROUP_NAME, [
  createRovingFocusGroupScope
]);
var useRovingFocusGroupScope = createRovingFocusGroupScope();
var ToggleGroup = import_react3.default.forwardRef((props, forwardedRef) => {
  const { type, ...toggleGroupProps } = props;
  if (type === "single") {
    const singleProps = toggleGroupProps;
    return (0, import_jsx_runtime9.jsx)(ToggleGroupImplSingle, { ...singleProps, ref: forwardedRef });
  }
  if (type === "multiple") {
    const multipleProps = toggleGroupProps;
    return (0, import_jsx_runtime9.jsx)(ToggleGroupImplMultiple, { ...multipleProps, ref: forwardedRef });
  }
  throw new Error(`Missing prop \`type\` expected on \`${TOGGLE_GROUP_NAME}\``);
});
ToggleGroup.displayName = TOGGLE_GROUP_NAME;
var [ToggleGroupValueProvider, useToggleGroupValueContext] = createToggleGroupContext(TOGGLE_GROUP_NAME);
var ToggleGroupImplSingle = import_react3.default.forwardRef((props, forwardedRef) => {
  const {
    value: valueProp,
    defaultValue,
    onValueChange = () => {
    },
    ...toggleGroupSingleProps
  } = props;
  const [value, setValue] = useControllableState({
    prop: valueProp,
    defaultProp: defaultValue ?? "",
    onChange: onValueChange,
    caller: TOGGLE_GROUP_NAME
  });
  return (0, import_jsx_runtime9.jsx)(
    ToggleGroupValueProvider,
    {
      scope: props.__scopeToggleGroup,
      type: "single",
      value: import_react3.default.useMemo(() => value ? [value] : [], [value]),
      onItemActivate: setValue,
      onItemDeactivate: import_react3.default.useCallback(() => setValue(""), [setValue]),
      children: (0, import_jsx_runtime9.jsx)(ToggleGroupImpl, { ...toggleGroupSingleProps, ref: forwardedRef })
    }
  );
});
var ToggleGroupImplMultiple = import_react3.default.forwardRef((props, forwardedRef) => {
  const {
    value: valueProp,
    defaultValue,
    onValueChange = () => {
    },
    ...toggleGroupMultipleProps
  } = props;
  const [value, setValue] = useControllableState({
    prop: valueProp,
    defaultProp: defaultValue ?? [],
    onChange: onValueChange,
    caller: TOGGLE_GROUP_NAME
  });
  const handleButtonActivate = import_react3.default.useCallback(
    (itemValue) => setValue((prevValue = []) => [...prevValue, itemValue]),
    [setValue]
  );
  const handleButtonDeactivate = import_react3.default.useCallback(
    (itemValue) => setValue((prevValue = []) => prevValue.filter((value2) => value2 !== itemValue)),
    [setValue]
  );
  return (0, import_jsx_runtime9.jsx)(
    ToggleGroupValueProvider,
    {
      scope: props.__scopeToggleGroup,
      type: "multiple",
      value,
      onItemActivate: handleButtonActivate,
      onItemDeactivate: handleButtonDeactivate,
      children: (0, import_jsx_runtime9.jsx)(ToggleGroupImpl, { ...toggleGroupMultipleProps, ref: forwardedRef })
    }
  );
});
ToggleGroup.displayName = TOGGLE_GROUP_NAME;
var [ToggleGroupContext, useToggleGroupContext] = createToggleGroupContext(TOGGLE_GROUP_NAME);
var ToggleGroupImpl = import_react3.default.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeToggleGroup,
      disabled = false,
      rovingFocus = true,
      orientation,
      dir,
      loop = true,
      ...toggleGroupProps
    } = props;
    const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeToggleGroup);
    const direction = useDirection(dir);
    const commonProps = { role: "group", dir: direction, ...toggleGroupProps };
    return (0, import_jsx_runtime9.jsx)(ToggleGroupContext, { scope: __scopeToggleGroup, rovingFocus, disabled, children: rovingFocus ? (0, import_jsx_runtime9.jsx)(
      Root,
      {
        asChild: true,
        ...rovingFocusGroupScope,
        orientation,
        dir: direction,
        loop,
        children: (0, import_jsx_runtime9.jsx)(Primitive.div, { ...commonProps, ref: forwardedRef })
      }
    ) : (0, import_jsx_runtime9.jsx)(Primitive.div, { ...commonProps, ref: forwardedRef }) });
  }
);
var ITEM_NAME2 = "ToggleGroupItem";
var ToggleGroupItem = import_react3.default.forwardRef(
  (props, forwardedRef) => {
    const valueContext = useToggleGroupValueContext(ITEM_NAME2, props.__scopeToggleGroup);
    const context = useToggleGroupContext(ITEM_NAME2, props.__scopeToggleGroup);
    const rovingFocusGroupScope = useRovingFocusGroupScope(props.__scopeToggleGroup);
    const pressed = valueContext.value.includes(props.value);
    const disabled = context.disabled || props.disabled;
    const commonProps = { ...props, pressed, disabled };
    const ref = import_react3.default.useRef(null);
    return context.rovingFocus ? (0, import_jsx_runtime9.jsx)(
      Item,
      {
        asChild: true,
        ...rovingFocusGroupScope,
        focusable: !disabled,
        active: pressed,
        ref,
        children: (0, import_jsx_runtime9.jsx)(ToggleGroupItemImpl, { ...commonProps, ref: forwardedRef })
      }
    ) : (0, import_jsx_runtime9.jsx)(ToggleGroupItemImpl, { ...commonProps, ref: forwardedRef });
  }
);
ToggleGroupItem.displayName = ITEM_NAME2;
var ToggleGroupItemImpl = import_react3.default.forwardRef(
  (props, forwardedRef) => {
    const { __scopeToggleGroup, value, ...itemProps } = props;
    const valueContext = useToggleGroupValueContext(ITEM_NAME2, __scopeToggleGroup);
    const singleProps = { role: "radio", "aria-checked": props.pressed, "aria-pressed": void 0 };
    const typeProps = valueContext.type === "single" ? singleProps : void 0;
    return (0, import_jsx_runtime9.jsx)(
      Toggle,
      {
        ...typeProps,
        ...itemProps,
        ref: forwardedRef,
        onPressedChange: (pressed) => {
          if (pressed) {
            valueContext.onItemActivate(value);
          } else {
            valueContext.onItemDeactivate(value);
          }
        }
      }
    );
  }
);
var Root2 = ToggleGroup;
var Item2 = ToggleGroupItem;

// node_modules/@strapi/upload/dist/admin/future/services/folders.mjs
var foldersApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    getFolders: builder.query({
      query: (params = {}) => {
        const { parentId } = params;
        const queryParams = {};
        if (parentId != null) {
          queryParams["filters"] = {
            $and: [
              {
                parent: {
                  id: parentId
                }
              }
            ]
          };
        } else {
          queryParams["filters"] = {
            $and: [
              {
                parent: {
                  id: {
                    $null: true
                  }
                }
              }
            ]
          };
        }
        return {
          url: "/upload/folders",
          method: "GET",
          config: {
            params: queryParams
          }
        };
      },
      transformResponse: (response) => (
        // TODO dont want this cast
        response.data
      ),
      providesTags: (results) => {
        if (results) {
          return [
            ...results.map(({ id }) => ({
              type: "Folder",
              id
            })),
            {
              type: "Folder",
              id: "LIST"
            }
          ];
        }
        return [
          {
            type: "Folder",
            id: "LIST"
          }
        ];
      }
    }),
    createFolder: builder.mutation({
      query: (body) => ({
        url: "/upload/folders",
        method: "POST",
        data: body
      }),
      transformResponse: (response) => response.data,
      invalidatesTags: [
        {
          type: "Folder",
          id: "LIST"
        }
      ]
    }),
    getFolder: builder.query({
      query: ({ id }) => ({
        url: `/upload/folders/${id}`,
        method: "GET",
        config: {
          params: {
            populate: {
              parent: {
                populate: {
                  parent: "*"
                }
              },
              children: {
                count: true
              },
              files: {
                count: true
              }
            }
          }
        }
      }),
      transformResponse: (response) => response.data,
      providesTags: (_result, _error, { id }) => [
        {
          type: "Folder",
          id
        }
      ]
    })
  })
});
var { useCreateFolderMutation, useGetFoldersQuery, useGetFolderQuery } = foldersApi;

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetDetailsDrawer.mjs
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var React17 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/enums.mjs
var AssetType = function(AssetType2) {
  AssetType2["Video"] = "video";
  AssetType2["Image"] = "image";
  AssetType2["Document"] = "doc";
  AssetType2["Audio"] = "audio";
  return AssetType2;
}({});

// node_modules/@strapi/upload/dist/admin/future/services/assets.mjs
var assetsApi = uploadApi.injectEndpoints({
  endpoints: (builder) => ({
    getAssets: builder.query({
      query: (params = {}) => {
        const { folder, ...rest } = params;
        const queryParams = {
          ...rest
        };
        if (folder != null) {
          queryParams["filters"] = {
            $and: [
              {
                folder: {
                  id: folder
                }
              }
            ]
          };
        } else {
          queryParams["filters"] = {
            $and: [
              {
                folder: {
                  id: {
                    $null: true
                  }
                }
              }
            ]
          };
        }
        return {
          url: "/upload/files",
          method: "GET",
          config: {
            params: queryParams
          }
        };
      },
      transformResponse: (response) => response,
      providesTags: (result) => result ? [
        ...result.results.map(({ id }) => ({
          type: "Asset",
          id
        })),
        {
          type: "Asset",
          id: "LIST"
        }
      ] : [
        {
          type: "Asset",
          id: "LIST"
        }
      ]
    }),
    getAsset: builder.query({
      query: (id) => ({
        url: `/upload/files/${id}`,
        method: "GET"
      }),
      providesTags: (_result, _error, id) => [
        {
          type: "Asset",
          id
        }
      ]
    })
  })
});
var { useGetAssetsQuery, useGetAssetQuery } = assetsApi;

// node_modules/@strapi/upload/dist/admin/future/utils/getAssetIcon.mjs
var DOC_ICON_MAP = {
  pdf: W5,
  csv: I5,
  xls: Z5,
  zip: Q5
};
var getAssetIcon = (mime, ext) => {
  const fileExtension = getFileExtension(ext);
  if (mime == null ? void 0 : mime.includes(AssetType.Image)) {
    return v3;
  }
  if (mime == null ? void 0 : mime.includes(AssetType.Video)) {
    return an;
  }
  if (mime == null ? void 0 : mime.includes(AssetType.Audio)) {
    return e3;
  }
  return fileExtension ? DOC_ICON_MAP[fileExtension] || O5 : O5;
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetPreview.mjs
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var React16 = __toESM(require_react(), 1);
var PreviewContainer = gt(R)`
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  max-height: 24rem;
  overflow: hidden;
  border-radius: ${({ theme }) => theme.borderRadius};
  padding: ${({ theme }) => theme.spaces[3]};
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      transparent 0% 50%
    )
    50% / 20px 20px;
`;
var AssetContainer = gt(T)`
  justify-content: center;
  position: relative;
  z-index: 2;
  width: 100%;
  height: 100%;
`;
var StyledImage = gt.img`
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
`;
var StyledVideo = gt.video`
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
`;
var StyledAudio = gt.audio`
  width: 100%;
`;
var StyledPdfIframe = gt.iframe`
  width: 100%;
  height: 100%;
  min-height: 200px;
  border: none;
`;
var IconFallback = gt(T)`
  height: 100%;
  aspect-ratio: 1;
  width: auto;
  max-width: 100%;
  margin: 0 auto;
  color: ${({ theme }) => theme.colors.neutral500};
  background: ${({ theme }) => theme.colors.neutral150};
`;
var LoaderOverlay = gt(T)`
  position: absolute;
  inset: 0;
  z-index: 1;
`;
var AssetLoader = () => {
  const { formatMessage } = useIntl();
  return (0, import_jsx_runtime10.jsx)(LoaderOverlay, {
    justifyContent: "center",
    alignItems: "center",
    children: (0, import_jsx_runtime10.jsx)(Fn, {
      children: formatMessage({
        id: "app.loading",
        defaultMessage: "Loading..."
      })
    })
  });
};
var AssetPreview = ({ asset }) => {
  const { formatMessage } = useIntl();
  const { alternativeText, ext, mime, url } = asset;
  const mediaUrl = prefixFileUrlWithBackendUrl(url);
  const [isMediaLoaded, setIsMediaLoaded] = React16.useState(false);
  React16.useEffect(() => {
    setIsMediaLoaded(false);
  }, [
    mediaUrl
  ]);
  if (mime == null ? void 0 : mime.includes(AssetType.Image)) {
    const imageUrl = prefixFileUrlWithBackendUrl(url);
    if (imageUrl) {
      return (0, import_jsx_runtime10.jsxs)(PreviewContainer, {
        children: [
          !isMediaLoaded && (0, import_jsx_runtime10.jsx)(AssetLoader, {}),
          (0, import_jsx_runtime10.jsx)(AssetContainer, {
            children: (0, import_jsx_runtime10.jsx)(StyledImage, {
              src: imageUrl,
              alt: alternativeText || asset.name || "",
              onLoad: () => setIsMediaLoaded(true),
              onError: () => setIsMediaLoaded(true)
            })
          })
        ]
      });
    }
  }
  if ((mime == null ? void 0 : mime.includes(AssetType.Video)) && mediaUrl) {
    return (0, import_jsx_runtime10.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime10.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime10.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime10.jsx)(StyledVideo, {
            src: mediaUrl,
            controls: true,
            title: asset.name,
            onLoadedData: () => setIsMediaLoaded(true),
            onError: () => setIsMediaLoaded(true),
            children: formatMessage({
              id: getTranslationKey("asset-details.videoNotSupported"),
              defaultMessage: "Your browser does not support the video tag."
            })
          })
        })
      ]
    });
  }
  if ((mime == null ? void 0 : mime.includes(AssetType.Audio)) && mediaUrl) {
    return (0, import_jsx_runtime10.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime10.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime10.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime10.jsx)(T, {
            width: "100%",
            padding: 4,
            justifyContent: "center",
            alignItems: "center",
            height: "100%",
            minHeight: "12rem",
            children: (0, import_jsx_runtime10.jsx)(StyledAudio, {
              src: mediaUrl,
              controls: true,
              onLoadedData: () => setIsMediaLoaded(true),
              onError: () => setIsMediaLoaded(true)
            })
          })
        })
      ]
    });
  }
  const isPdf = (ext == null ? void 0 : ext.toLowerCase()) === "pdf" || (ext == null ? void 0 : ext.toLowerCase()) === ".pdf" || mime === "application/pdf";
  if (isPdf && mediaUrl) {
    return (0, import_jsx_runtime10.jsxs)(PreviewContainer, {
      children: [
        !isMediaLoaded && (0, import_jsx_runtime10.jsx)(AssetLoader, {}),
        (0, import_jsx_runtime10.jsx)(AssetContainer, {
          children: (0, import_jsx_runtime10.jsx)(StyledPdfIframe, {
            src: `${mediaUrl}#toolbar=0`,
            title: asset.name,
            onLoad: () => setIsMediaLoaded(true)
          })
        })
      ]
    });
  }
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime10.jsx)(PreviewContainer, {
    children: (0, import_jsx_runtime10.jsxs)(IconFallback, {
      justifyContent: "center",
      alignItems: "center",
      gap: 1,
      direction: "column",
      hasRadius: true,
      children: [
        (0, import_jsx_runtime10.jsx)(DocIcon, {
          width: 24,
          height: 24
        }),
        (0, import_jsx_runtime10.jsx)(I, {
          variant: "pi",
          children: formatMessage({
            id: getTranslationKey("asset-details.noPreview"),
            defaultMessage: "No preview available"
          })
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetDetails/AssetDetailsDrawer.mjs
var URL_PARAM = "assetId";
var useAssetDetailsParam = () => {
  const [{ query }, setQuery] = useQueryParams();
  const detailsId = query == null ? void 0 : query[URL_PARAM];
  const assetId = detailsId ? parseInt(detailsId, 10) : null;
  const hasValidId = assetId !== null && !Number.isNaN(assetId);
  const [isClosing, setIsClosing] = React17.useState(false);
  const displayAssetId = React17.useRef(null);
  const isVisible = hasValidId && !isClosing;
  React17.useEffect(() => {
    if (hasValidId) {
      displayAssetId.current = assetId;
    }
  }, [
    hasValidId,
    assetId
  ]);
  const openDetails = React17.useCallback((id) => {
    setIsClosing(false);
    setQuery({
      [URL_PARAM]: String(id)
    });
  }, [
    setQuery
  ]);
  const closeDetails = React17.useCallback(() => {
    if (!hasValidId) return;
    setIsClosing(true);
  }, [
    hasValidId
  ]);
  React17.useEffect(() => {
    if (!isClosing) return;
    const timer = window.setTimeout(() => {
      setQuery({
        [URL_PARAM]: void 0
      }, "remove");
      setIsClosing(false);
      displayAssetId.current = null;
    }, DRAWER_CLOSE_ANIMATION_MS);
    return () => window.clearTimeout(timer);
  }, [
    isClosing,
    setQuery
  ]);
  const shouldRenderDrawer = hasValidId || isClosing;
  const drawerAssetId = isClosing ? displayAssetId.current ?? assetId : assetId;
  return {
    assetId: drawerAssetId,
    isVisible,
    shouldRenderDrawer,
    openDetails,
    closeDetails
  };
};
var DetailItemContainer = gt(T)`
  flex: 0 0 calc(50% - ${({ theme }) => theme.spaces[2]});
`;
var DetailItem = ({ label, value }) => (0, import_jsx_runtime11.jsxs)(DetailItemContainer, {
  direction: "column",
  justifyContent: "flex-start",
  alignItems: "flex-start",
  gap: 1,
  children: [
    (0, import_jsx_runtime11.jsx)(I, {
      variant: "sigma",
      textColor: "neutral600",
      fontWeight: "semiBold",
      textTransform: "uppercase",
      children: label
    }),
    (0, import_jsx_runtime11.jsx)(I, {
      variant: "pi",
      textColor: "neutral700",
      children: value ?? "-"
    })
  ]
});
var StyledWarning = gt(Nn)`
  width: 1.6rem;
  height: 1.6rem;

  path {
    fill: ${({ theme }) => theme.colors.warning500};
  }
`;
var DetailField = ({ name, label, value, required }) => (0, import_jsx_runtime11.jsxs)(Vm.Root, {
  name,
  required,
  children: [
    (0, import_jsx_runtime11.jsx)(Vm.Label, {
      children: label
    }),
    (0, import_jsx_runtime11.jsx)(X0, {
      value: value ?? "",
      // TODO: handle onChange
      onChange: () => {
      },
      endAction: !value ? (0, import_jsx_runtime11.jsx)(StyledWarning, {}) : void 0,
      type: "text"
    })
  ]
});
var AssetDetails = ({ asset }) => {
  var _a;
  const { formatMessage, formatDate } = useIntl();
  const isImage = (_a = asset.mime) == null ? void 0 : _a.includes(AssetType.Image);
  return (0, import_jsx_runtime11.jsxs)(T, {
    direction: "column",
    alignItems: "stretch",
    gap: 4,
    paddingTop: 4,
    paddingBottom: 4,
    paddingLeft: 5,
    paddingRight: 5,
    children: [
      (0, import_jsx_runtime11.jsx)(I, {
        variant: "beta",
        fontWeight: "semiBold",
        tag: "h3",
        children: formatMessage({
          id: getTranslationKey("asset-details.fileInfo"),
          defaultMessage: "File info"
        })
      }),
      (0, import_jsx_runtime11.jsxs)(T, {
        wrap: "wrap",
        gap: 4,
        background: "neutral100",
        paddingTop: 4,
        paddingBottom: 4,
        paddingLeft: 6,
        paddingRight: 6,
        alignItems: "flex-start",
        children: [
          (0, import_jsx_runtime11.jsx)(DetailItem, {
            label: formatMessage({
              id: getTranslationKey("asset-details.creationDate"),
              defaultMessage: "Creation date"
            }),
            value: asset.createdAt ? formatDate(new Date(asset.createdAt), {
              dateStyle: "long",
              timeStyle: "short"
            }) : null
          }),
          (0, import_jsx_runtime11.jsx)(DetailItem, {
            label: formatMessage({
              id: getTranslationKey("asset-details.lastUpdated"),
              defaultMessage: "Last updated"
            }),
            value: asset.updatedAt ? formatDate(new Date(asset.updatedAt), {
              dateStyle: "long",
              timeStyle: "short"
            }) : null
          }),
          (0, import_jsx_runtime11.jsx)(DetailItem, {
            label: formatMessage({
              id: getTranslationKey("asset-details.createdBy"),
              defaultMessage: "Created by"
            }),
            value: asset.createdBy ? getDisplayName({
              firstname: asset.createdBy.firstname ?? void 0,
              lastname: asset.createdBy.lastname ?? void 0,
              username: asset.createdBy.username ?? void 0,
              email: asset.createdBy.email ?? void 0
            }) ?? "-" : null
          }),
          (0, import_jsx_runtime11.jsx)(DetailItem, {
            label: formatMessage({
              id: getTranslationKey("asset-details.size"),
              defaultMessage: "Size"
            }),
            value: asset.size ? formatBytes(asset.size, 1) : null
          }),
          isImage && (asset.width != null || asset.height != null) && (0, import_jsx_runtime11.jsx)(DetailItem, {
            label: formatMessage({
              id: getTranslationKey("asset-details.dimensions"),
              defaultMessage: "Dimensions"
            }),
            value: asset.width != null && asset.height != null ? `${asset.width} × ${asset.height}` : null
          }),
          (0, import_jsx_runtime11.jsx)(DetailItem, {
            label: formatMessage({
              id: getTranslationKey("asset-details.extension"),
              defaultMessage: "Extension"
            }),
            value: getFileExtension(asset.ext)
          }),
          (0, import_jsx_runtime11.jsx)(DetailItem, {
            label: formatMessage({
              id: getTranslationKey("asset-details.assetId"),
              defaultMessage: "Asset ID"
            }),
            value: String(asset.id)
          })
        ]
      }),
      (0, import_jsx_runtime11.jsx)(DetailField, {
        name: "fileName",
        label: formatMessage({
          id: getTranslationKey("asset-details.fileName"),
          defaultMessage: "File name"
        }),
        value: asset.name,
        required: true
      }),
      isImage && (0, import_jsx_runtime11.jsxs)(import_jsx_runtime11.Fragment, {
        children: [
          (0, import_jsx_runtime11.jsx)(DetailField, {
            name: "caption",
            label: formatMessage({
              id: getTranslationKey("asset-details.caption"),
              defaultMessage: "Caption"
            }),
            value: asset.caption
          }),
          (0, import_jsx_runtime11.jsx)(DetailField, {
            name: "alternativeText",
            label: formatMessage({
              id: getTranslationKey("asset-details.alternativeText"),
              defaultMessage: "Alternative text"
            }),
            value: asset.alternativeText
          })
        ]
      })
    ]
  });
};
var DrawerHeader = ({ asset, closeDetails }) => {
  const DocIcon = asset ? getAssetIcon(asset.mime, asset.ext) : G5;
  return (0, import_jsx_runtime11.jsxs)(T, {
    gap: 2,
    paddingLeft: 5,
    paddingTop: 3,
    paddingBottom: 3,
    paddingRight: 3,
    children: [
      (0, import_jsx_runtime11.jsx)(DocIcon, {
        width: 20,
        height: 20
      }),
      (0, import_jsx_runtime11.jsx)(Drawer.Title, {
        asChild: true,
        children: (0, import_jsx_runtime11.jsx)(I, {
          variant: "omega",
          fontWeight: "semiBold",
          overflow: "hidden",
          ellipsis: true,
          tag: "h2",
          children: asset.name
        })
      }),
      (0, import_jsx_runtime11.jsx)(R, {
        marginLeft: "auto",
        children: (0, import_jsx_runtime11.jsx)(Drawer.CloseButton, {
          onClose: closeDetails,
          children: (0, import_jsx_runtime11.jsx)(D2, {})
        })
      })
    ]
  });
};
var DrawerContent = ({ assetId, closeDetails }) => {
  const { formatMessage } = useIntl();
  const { data: asset, isLoading, error } = useGetAssetQuery(assetId, {
    refetchOnMountOrArgChange: false,
    refetchOnReconnect: false,
    refetchOnFocus: false
  });
  if (isLoading) {
    return (0, import_jsx_runtime11.jsx)(T, {
      justifyContent: "center",
      padding: 8,
      children: (0, import_jsx_runtime11.jsx)(Fn, {
        children: formatMessage({
          id: "app.loading",
          defaultMessage: "Loading..."
        })
      })
    });
  }
  if (error || !asset) {
    return (0, import_jsx_runtime11.jsx)(T, {
      direction: "column",
      alignItems: "stretch",
      gap: 4,
      padding: 4,
      children: (0, import_jsx_runtime11.jsx)(Cm, {
        variant: "danger",
        closeLabel: formatMessage({
          id: "global.close",
          defaultMessage: "Close"
        }),
        onClose: closeDetails,
        children: formatMessage({
          id: getTranslationKey("asset-details.error"),
          defaultMessage: "Failed to load file details."
        })
      })
    });
  }
  return (0, import_jsx_runtime11.jsxs)(import_jsx_runtime11.Fragment, {
    children: [
      (0, import_jsx_runtime11.jsx)(DrawerHeader, {
        asset,
        closeDetails
      }),
      (0, import_jsx_runtime11.jsxs)(Drawer.ScrollableContent, {
        children: [
          (0, import_jsx_runtime11.jsx)(AssetPreview, {
            asset
          }),
          (0, import_jsx_runtime11.jsx)(AssetDetails, {
            asset
          })
        ]
      })
    ]
  });
};
var AssetDetailsDrawer = () => {
  const { formatMessage } = useIntl();
  const { assetId, isVisible, shouldRenderDrawer, closeDetails } = useAssetDetailsParam();
  if (!shouldRenderDrawer || assetId === null) {
    return null;
  }
  return (0, import_jsx_runtime11.jsxs)(Drawer.Root, {
    isVisible,
    onClose: closeDetails,
    children: [
      (0, import_jsx_runtime11.jsx)("div", {
        children: (0, import_jsx_runtime11.jsxs)(at, {
          children: [
            (0, import_jsx_runtime11.jsx)(Drawer.Title, {
              children: formatMessage({
                id: getTranslationKey("asset-details.title"),
                defaultMessage: "File details"
              })
            }),
            (0, import_jsx_runtime11.jsx)(Drawer.Description, {
              children: formatMessage({
                id: getTranslationKey("asset-details.description"),
                defaultMessage: "Displays file information and metadata"
              })
            })
          ]
        })
      }),
      (0, import_jsx_runtime11.jsx)(Drawer.Body, {
        animationDirection: "left",
        width: "41.6rem",
        height: "100vh",
        children: (0, import_jsx_runtime11.jsx)(DrawerContent, {
          assetId,
          closeDetails
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsGrid.mjs
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useFolderNavigation.mjs
var useFolderNavigation = () => {
  const [{ query }, setQuery] = useQueryParams();
  const currentFolderId = (query == null ? void 0 : query.folder) ? Number(query.folder) : null;
  const navigateToFolder = (folder) => {
    setQuery({
      folder: String(folder.id)
    });
  };
  return {
    currentFolderId,
    navigateToFolder
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsGrid.mjs
var StyledCard = gt(Rm)`
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: 8px;
  overflow: hidden;
  cursor: pointer;

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
  }
`;
var FoldersRow = gt(R)`
  grid-column: 1 / -1;
`;
var StyledFolderCard = gt(T)`
  width: 100%;
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[3]}`}; // 8px 12px
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]}; // 8px
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: ${({ theme }) => theme.borderRadius};
  background: ${({ theme }) => theme.colors.neutral0};
  cursor: pointer;
  transition: background 0.2s;

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: 2px;
  }
`;
var FolderIconContainer = gt(T)`
  flex-shrink: 0;
  color: ${({ theme }) => theme.colors.neutral600};
`;
var FolderName = gt(I)`
  flex: 1;
  min-width: 0;
`;
var FolderCard = ({ folder }) => {
  const { formatMessage } = useIntl();
  const { navigateToFolder } = useFolderNavigation();
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      navigateToFolder(folder);
    }
  };
  return (0, import_jsx_runtime12.jsxs)(StyledFolderCard, {
    onClick: () => navigateToFolder(folder),
    onKeyDown: handleKeyDown,
    role: "listitem",
    tabIndex: 0,
    children: [
      (0, import_jsx_runtime12.jsx)(FolderIconContainer, {
        children: (0, import_jsx_runtime12.jsx)(K5, {
          width: 20,
          height: 20
        })
      }),
      (0, import_jsx_runtime12.jsx)(FolderName, {
        textColor: "neutral800",
        ellipsis: true,
        children: folder.name
      }),
      (0, import_jsx_runtime12.jsx)(st, {
        label: formatMessage({
          id: getTranslationKey("control-card.more-actions"),
          defaultMessage: "More actions"
        }),
        variant: "ghost",
        onClick: (e) => e.stopPropagation(),
        children: (0, import_jsx_runtime12.jsx)(E3, {})
      })
    ]
  });
};
var PreviewContainer2 = gt(R)`
  position: relative;
  width: 100%;
  padding-bottom: 62.5%;
  height: 0;
  overflow: hidden;
  background: repeating-conic-gradient(
      ${({ theme }) => theme.colors.neutral100} 0% 25%,
      transparent 0% 50%
    )
    50% / 20px 20px;
`;
var StyledImage2 = gt.img`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
`;
var IconPreview = gt(T)`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  color: ${({ theme }) => theme.colors.neutral500};
  background: ${({ theme }) => theme.colors.neutral100};
`;
var AssetPreview2 = ({ asset }) => {
  var _a;
  const { alternativeText, ext, formats, mime, url } = asset;
  if (mime == null ? void 0 : mime.includes(AssetType.Image)) {
    const mediaURL = prefixFileUrlWithBackendUrl((_a = formats == null ? void 0 : formats.thumbnail) == null ? void 0 : _a.url) ?? prefixFileUrlWithBackendUrl(url);
    if (mediaURL) {
      return (0, import_jsx_runtime12.jsx)(PreviewContainer2, {
        children: (0, import_jsx_runtime12.jsx)(StyledImage2, {
          src: mediaURL,
          alt: alternativeText || ""
        })
      });
    }
  }
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime12.jsx)(PreviewContainer2, {
    children: (0, import_jsx_runtime12.jsx)(IconPreview, {
      justifyContent: "center",
      alignItems: "center",
      children: (0, import_jsx_runtime12.jsx)(DocIcon, {
        width: 48,
        height: 48
      })
    })
  });
};
var StyledCardHeader = gt(Pm)`
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral200};
`;
var CardFooter = gt(T)`
  min-width: 0;
  width: 100%;
`;
var FileTypeIcon = gt(T)`
  color: ${({ theme }) => theme.colors.neutral600};
  flex-shrink: 0;
`;
var FileName = gt(I)`
  flex: 1;
  min-width: 0;
`;
var AssetCard = ({ asset, onAssetItemClick }) => {
  const { formatMessage } = useIntl();
  const TypeIcon = getAssetIcon(asset.mime, asset.ext);
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      onAssetItemClick(asset.id);
    }
  };
  return (0, import_jsx_runtime12.jsxs)(StyledCard, {
    tabIndex: 0,
    role: "listitem",
    onClick: () => onAssetItemClick(asset.id),
    onKeyDown: handleKeyDown,
    children: [
      (0, import_jsx_runtime12.jsx)(StyledCardHeader, {
        children: (0, import_jsx_runtime12.jsx)(AssetPreview2, {
          asset
        })
      }),
      (0, import_jsx_runtime12.jsx)(Om, {
        children: (0, import_jsx_runtime12.jsxs)(CardFooter, {
          alignItems: "center",
          gap: 2,
          children: [
            (0, import_jsx_runtime12.jsx)(FileTypeIcon, {
              children: (0, import_jsx_runtime12.jsx)(TypeIcon, {
                width: 20,
                height: 20
              })
            }),
            (0, import_jsx_runtime12.jsx)(FileName, {
              textColor: "primary800",
              ellipsis: true,
              children: asset.name
            }),
            (0, import_jsx_runtime12.jsx)(st, {
              label: formatMessage({
                id: getTranslationKey("control-card.more-actions"),
                defaultMessage: "More actions"
              }),
              variant: "ghost",
              children: (0, import_jsx_runtime12.jsx)(E3, {})
            })
          ]
        })
      })
    ]
  });
};
var AssetsGrid = ({ assets, folders = [], onAssetItemClick }) => {
  const { formatMessage } = useIntl();
  const totalItems = folders.length + assets.length;
  if (totalItems === 0) {
    return (0, import_jsx_runtime12.jsx)(R, {
      padding: 8,
      children: (0, import_jsx_runtime12.jsx)(I, {
        textColor: "neutral600",
        children: formatMessage({
          id: "app.components.EmptyStateLayout.content-document",
          defaultMessage: "No content found"
        })
      })
    });
  }
  return (0, import_jsx_runtime12.jsxs)(Um.Root, {
    gap: 4,
    role: "list",
    children: [
      folders.length > 0 && (0, import_jsx_runtime12.jsx)(FoldersRow, {
        children: (0, import_jsx_runtime12.jsx)(Um.Root, {
          gap: 4,
          children: folders.map((folder) => (0, import_jsx_runtime12.jsx)(Um.Item, {
            col: 3,
            m: 4,
            s: 6,
            xs: 12,
            children: (0, import_jsx_runtime12.jsx)(FolderCard, {
              folder
            })
          }, `folder-${folder.id}`))
        })
      }),
      assets.map((asset) => (0, import_jsx_runtime12.jsx)(Um.Item, {
        col: 3,
        m: 4,
        s: 6,
        xs: 12,
        direction: "column",
        alignItems: "stretch",
        children: (0, import_jsx_runtime12.jsx)(AssetCard, {
          asset,
          onAssetItemClick
        })
      }, asset.id))
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsTable.mjs
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/constants.mjs
var localStorageKeys = {
  view: `STRAPI_UPLOAD_LIBRARY_VIEW`
};
var viewOptions = {
  GRID: 0,
  TABLE: 1
};
var TABLE_HEADERS = [
  {
    name: "name",
    label: {
      id: getTranslationKey("list.table.header.name"),
      defaultMessage: "name"
    }
  },
  {
    name: "createdAt",
    label: {
      id: getTranslationKey("list.table.header.creationDate"),
      defaultMessage: "creation date"
    }
  },
  {
    name: "updatedAt",
    label: {
      id: getTranslationKey("list.table.header.lastModified"),
      defaultMessage: "last modified"
    }
  },
  {
    name: "size",
    label: {
      id: getTranslationKey("list.table.header.size"),
      defaultMessage: "size"
    }
  },
  {
    name: "actions",
    label: {
      id: getTranslationKey("list.table.header.actions"),
      defaultMessage: "actions"
    },
    isVisuallyHidden: true
  }
];

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/AssetsTable.mjs
var StyledTable = gt(i0)`
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  border: 1px solid ${({ theme }) => theme.colors.neutral150};
  border-radius: 4px;
  overflow: hidden;
`;
var StyledThead = gt(r0)`
  background: ${({ theme }) => theme.colors.neutral100};

  tr {
    border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
  }
`;
var StyledTh = gt(n0)`
  height: 40px;
  padding: 0 ${({ theme }) => theme.spaces[4]};
  text-align: left;
`;
var StyledTd = gt(Na)`
  padding: 0 ${({ theme }) => theme.spaces[4]};
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var StyledTr = gt(s0)`
  height: 48px;
  background: ${({ theme }) => theme.colors.neutral0};
  cursor: pointer;

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &:focus-visible {
    outline: 2px solid ${({ theme }) => theme.colors.primary600};
    outline-offset: -2px;
  }

  &:last-child {
    ${StyledTd} {
      border-bottom: 0;
    }
  }
`;
var StyledBodyTd = gt(Na)`
  padding: ${({ theme }) => theme.spaces[4]};
  border-bottom: 1px solid ${({ theme }) => theme.colors.neutral150};
`;
var AssetPreviewCell = ({ asset }) => {
  const { ext, mime } = asset;
  const DocIcon = getAssetIcon(mime, ext);
  return (0, import_jsx_runtime13.jsx)(T, {
    justifyContent: "center",
    alignItems: "center",
    borderRadius: "4px",
    color: "neutral500",
    width: "3.2rem",
    height: "3.2rem",
    shrink: 0,
    children: (0, import_jsx_runtime13.jsx)(DocIcon, {
      width: 20,
      height: 20
    })
  });
};
var AssetRow = ({ asset, onAssetItemClick }) => {
  const isMobile = useIsMobile();
  const { formatDate, formatMessage } = useIntl();
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      onAssetItemClick(asset.id);
    }
  };
  return (0, import_jsx_runtime13.jsxs)(StyledTr, {
    tabIndex: 0,
    role: "row",
    onClick: () => onAssetItemClick(asset.id),
    onKeyDown: handleKeyDown,
    children: [
      (0, import_jsx_runtime13.jsx)(StyledTd, {
        children: (0, import_jsx_runtime13.jsxs)(T, {
          gap: 3,
          alignItems: "center",
          children: [
            (0, import_jsx_runtime13.jsx)(AssetPreviewCell, {
              asset
            }),
            (0, import_jsx_runtime13.jsxs)(T, {
              direction: "column",
              alignItems: "flex-start",
              children: [
                (0, import_jsx_runtime13.jsx)(I, {
                  textColor: "neutral800",
                  fontWeight: "semiBold",
                  ellipsis: true,
                  children: asset.name
                }),
                isMobile && (0, import_jsx_runtime13.jsx)(I, {
                  textColor: "neutral600",
                  variant: "pi",
                  children: asset.size ? formatBytes(asset.size, 1) : "-"
                })
              ]
            })
          ]
        })
      }),
      !isMobile && (0, import_jsx_runtime13.jsxs)(import_jsx_runtime13.Fragment, {
        children: [
          (0, import_jsx_runtime13.jsx)(StyledTd, {
            children: (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral600",
              children: asset.createdAt ? formatDate(new Date(asset.createdAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime13.jsx)(StyledTd, {
            children: (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral600",
              children: asset.updatedAt ? formatDate(new Date(asset.updatedAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime13.jsx)(StyledTd, {
            children: (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral600",
              children: asset.size ? formatBytes(asset.size, 1) : "-"
            })
          })
        ]
      }),
      (0, import_jsx_runtime13.jsx)(StyledTd, {
        children: (0, import_jsx_runtime13.jsx)(T, {
          justifyContent: "flex-end",
          children: (0, import_jsx_runtime13.jsx)(st, {
            label: formatMessage({
              id: getTranslationKey("control-card.more-actions"),
              defaultMessage: "More actions"
            }),
            variant: "ghost",
            children: (0, import_jsx_runtime13.jsx)(E3, {})
          })
        })
      })
    ]
  });
};
var FolderTr = gt(StyledTr)`
  cursor: pointer;

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }
`;
var FolderRow = ({ folder }) => {
  const isMobile = useIsMobile();
  const { formatDate, formatMessage } = useIntl();
  const { navigateToFolder } = useFolderNavigation();
  const handleKeyDown = (e) => {
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      navigateToFolder(folder);
    }
  };
  return (0, import_jsx_runtime13.jsxs)(FolderTr, {
    tabIndex: 0,
    role: "row",
    onClick: () => navigateToFolder(folder),
    onKeyDown: handleKeyDown,
    children: [
      (0, import_jsx_runtime13.jsx)(StyledTd, {
        children: (0, import_jsx_runtime13.jsxs)(T, {
          gap: 3,
          alignItems: "center",
          children: [
            (0, import_jsx_runtime13.jsx)(T, {
              justifyContent: "center",
              alignItems: "center",
              borderRadius: "4px",
              color: "neutral600",
              width: "3.2rem",
              height: "3.2rem",
              shrink: 0,
              children: (0, import_jsx_runtime13.jsx)(K5, {
                width: 20,
                height: 20
              })
            }),
            (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral800",
              fontWeight: "semiBold",
              ellipsis: true,
              children: folder.name
            })
          ]
        })
      }),
      !isMobile && (0, import_jsx_runtime13.jsxs)(import_jsx_runtime13.Fragment, {
        children: [
          (0, import_jsx_runtime13.jsx)(StyledTd, {
            children: (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral600",
              children: folder.createdAt ? formatDate(new Date(folder.createdAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime13.jsx)(StyledTd, {
            children: (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral600",
              children: folder.updatedAt ? formatDate(new Date(folder.updatedAt), {
                dateStyle: "long"
              }) : "-"
            })
          }),
          (0, import_jsx_runtime13.jsx)(StyledTd, {
            children: (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral600",
              children: "-"
            })
          })
        ]
      }),
      (0, import_jsx_runtime13.jsx)(StyledTd, {
        children: (0, import_jsx_runtime13.jsx)(T, {
          justifyContent: "flex-end",
          children: (0, import_jsx_runtime13.jsx)(st, {
            label: formatMessage({
              id: getTranslationKey("control-card.more-actions"),
              defaultMessage: "More actions"
            }),
            variant: "ghost",
            onClick: (e) => e.stopPropagation(),
            children: (0, import_jsx_runtime13.jsx)(E3, {})
          })
        })
      })
    ]
  });
};
var AssetsTable = ({ assets, folders = [], onAssetItemClick }) => {
  const isMobile = useIsMobile();
  const { formatMessage } = useIntl();
  const visibleHeaders = isMobile ? TABLE_HEADERS.filter((h) => h.name === "name" || h.name === "actions") : TABLE_HEADERS;
  const totalRows = folders.length + assets.length;
  return (0, import_jsx_runtime13.jsxs)(StyledTable, {
    colCount: visibleHeaders.length,
    rowCount: totalRows + 1,
    children: [
      (0, import_jsx_runtime13.jsx)(StyledThead, {
        children: (0, import_jsx_runtime13.jsx)(s0, {
          children: visibleHeaders.map((header) => {
            const tableHeaderLabel = formatMessage(header.label);
            const isVisuallyHidden = "isVisuallyHidden" in header && header.isVisuallyHidden;
            if (isVisuallyHidden) {
              return (0, import_jsx_runtime13.jsx)(StyledTh, {
                children: (0, import_jsx_runtime13.jsx)(at, {
                  children: formatMessage({
                    id: getTranslationKey("table.header.actions"),
                    defaultMessage: "actions"
                  })
                })
              }, header.name);
            }
            return (0, import_jsx_runtime13.jsx)(StyledTh, {
              children: (0, import_jsx_runtime13.jsx)(I, {
                textColor: "neutral600",
                variant: "sigma",
                children: tableHeaderLabel
              })
            }, header.name);
          })
        })
      }),
      (0, import_jsx_runtime13.jsx)(o0, {
        children: totalRows === 0 ? (0, import_jsx_runtime13.jsx)(s0, {
          children: (0, import_jsx_runtime13.jsx)(StyledBodyTd, {
            colSpan: visibleHeaders.length,
            children: (0, import_jsx_runtime13.jsx)(I, {
              textColor: "neutral600",
              children: formatMessage({
                id: "app.components.EmptyStateLayout.content-document",
                defaultMessage: "No content found"
              })
            })
          })
        }) : (0, import_jsx_runtime13.jsxs)(import_jsx_runtime13.Fragment, {
          children: [
            folders.map((folder) => (0, import_jsx_runtime13.jsx)(FolderRow, {
              folder
            }, `folder-${folder.id}`)),
            assets.map((asset) => (0, import_jsx_runtime13.jsx)(AssetRow, {
              asset,
              onAssetItemClick
            }, asset.id))
          ]
        })
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/CreateFolderDialog.mjs
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var import_react4 = __toESM(require_react(), 1);
var StyledModalContent = gt(n1.Content)`
  max-width: 51.6rem;
`;
var CreateFolderDialog = ({ open, folderName, parentFolderId, onClose }) => {
  const { formatMessage } = useIntl();
  const { toggleNotification } = useNotification();
  const [name, setName] = (0, import_react4.useState)("");
  const [fieldError, setFieldError] = (0, import_react4.useState)();
  const [createFolder, { isLoading }] = useCreateFolderMutation();
  (0, import_react4.useEffect)(() => {
    if (open) {
      setName("");
      setFieldError(void 0);
    }
  }, [
    open
  ]);
  const handleSubmit = async (e) => {
    e.preventDefault();
    const trimmedName = name.trim();
    if (!trimmedName) {
      setFieldError(formatMessage({
        id: getTranslationKey("folder.create.form.error.name-required"),
        defaultMessage: "Name is required"
      }));
      return;
    }
    try {
      await createFolder({
        name: trimmedName,
        parent: parentFolderId
      }).unwrap();
      toggleNotification({
        type: "success",
        message: formatMessage({
          id: getTranslationKey("folder.create.success"),
          defaultMessage: "Folder has been created"
        })
      });
      onClose();
    } catch (err) {
      const apiError = err;
      if (apiError == null ? void 0 : apiError.message) {
        setFieldError(apiError.message);
      } else {
        toggleNotification({
          type: "danger",
          message: formatMessage({
            id: getTranslationKey("folder.create.form.error.unknown"),
            defaultMessage: "An error occurred while creating the folder"
          })
        });
      }
    }
  };
  return (0, import_jsx_runtime14.jsx)(n1.Root, {
    open,
    onOpenChange: onClose,
    children: (0, import_jsx_runtime14.jsxs)(StyledModalContent, {
      children: [
        (0, import_jsx_runtime14.jsx)(n1.Header, {
          children: (0, import_jsx_runtime14.jsx)(n1.Title, {
            children: formatMessage({
              id: getTranslationKey("folder.create.title-in"),
              defaultMessage: "New folder in {folderName}"
            }, {
              folderName
            })
          })
        }),
        (0, import_jsx_runtime14.jsxs)("form", {
          onSubmit: handleSubmit,
          children: [
            (0, import_jsx_runtime14.jsx)(n1.Body, {
              children: (0, import_jsx_runtime14.jsxs)(Vm.Root, {
                error: fieldError,
                name: "name",
                required: true,
                children: [
                  (0, import_jsx_runtime14.jsx)(Vm.Label, {
                    children: formatMessage({
                      id: getTranslationKey("folder.form.name.label"),
                      defaultMessage: "Folder name"
                    })
                  }),
                  (0, import_jsx_runtime14.jsx)(X0, {
                    value: name,
                    onChange: (e) => {
                      setName(e.target.value);
                      setFieldError(void 0);
                    },
                    autoFocus: true
                  }),
                  (0, import_jsx_runtime14.jsx)(Vm.Error, {})
                ]
              })
            }),
            (0, import_jsx_runtime14.jsx)(n1.Footer, {
              children: (0, import_jsx_runtime14.jsxs)(T, {
                gap: 2,
                justifyContent: "space-between",
                width: "100%",
                children: [
                  (0, import_jsx_runtime14.jsx)(zn, {
                    variant: "tertiary",
                    onClick: onClose,
                    type: "button",
                    children: formatMessage({
                      id: "app.components.Button.cancel",
                      defaultMessage: "Cancel"
                    })
                  }),
                  (0, import_jsx_runtime14.jsx)(zn, {
                    type: "submit",
                    loading: isLoading,
                    children: formatMessage({
                      id: getTranslationKey("folder.create.submit"),
                      defaultMessage: "Create folder"
                    })
                  })
                ]
              })
            })
          ]
        })
      ]
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZone.mjs
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
var React18 = __toESM(require_react(), 1);

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZoneContext.mjs
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
var import_react5 = __toESM(require_react(), 1);
var UploadDropZoneContext = (0, import_react5.createContext)(null);
var DropZoneWrapper = gt(R)`
  position: relative;
  display: flex;
  flex-direction: column;
  min-height: 100%;
`;
var UploadDropZoneProvider = ({ children, onDrop }) => {
  const [isDragging, setIsDragging] = (0, import_react5.useState)(false);
  const dragCounterRef = (0, import_react5.useRef)(0);
  const contextValue = {
    isDragging
  };
  (0, import_react5.useEffect)(() => {
    const handleDragEnd = () => {
      setIsDragging(false);
      dragCounterRef.current = 0;
    };
    const handleDocumentDragLeave = (e) => {
      if (!e.relatedTarget) {
        setIsDragging(false);
        dragCounterRef.current = 0;
      }
    };
    document.addEventListener("dragend", handleDragEnd);
    document.addEventListener("dragleave", handleDocumentDragLeave);
    return () => {
      document.removeEventListener("dragend", handleDragEnd);
      document.removeEventListener("dragleave", handleDocumentDragLeave);
    };
  }, []);
  const handleDragEnter = (0, import_react5.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    dragCounterRef.current += 1;
    if (e.dataTransfer.types.includes("Files")) {
      setIsDragging(true);
    }
  }, []);
  const handleDragLeave = (0, import_react5.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    dragCounterRef.current -= 1;
    if (dragCounterRef.current <= 0) {
      setIsDragging(false);
      dragCounterRef.current = 0;
    }
  }, []);
  const handleDragOver = (0, import_react5.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    e.dataTransfer.dropEffect = "copy";
  }, []);
  const handleDrop = (0, import_react5.useCallback)((e) => {
    e.preventDefault();
    e.stopPropagation();
    setIsDragging(false);
    dragCounterRef.current = 0;
    const { files } = e.dataTransfer;
    if ((files == null ? void 0 : files.length) && onDrop) {
      onDrop(Array.from(files));
    }
  }, [
    onDrop
  ]);
  return (0, import_jsx_runtime15.jsx)(UploadDropZoneContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime15.jsx)(DropZoneWrapper, {
      "data-testid": "assets-dropzone",
      onDragEnter: handleDragEnter,
      onDragLeave: handleDragLeave,
      onDragOver: handleDragOver,
      onDrop: handleDrop,
      children
    })
  });
};
var useUploadDropZone = () => {
  const context = (0, import_react5.useContext)(UploadDropZoneContext);
  if (!context) {
    throw new Error("useUploadDropZone must be used within UploadDropZone");
  }
  return {
    isDragging: context.isDragging
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/DropZone/UploadDropZone.mjs
var setOpacity = (hex, alpha) => `${hex}${Math.floor(alpha * 255).toString(16).padStart(2, "0")}`;
var DropZoneOverlay = gt(R)`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: ${({ theme }) => setOpacity(theme.colors.primary200, 0.3)};
  border: 1px solid ${({ theme }) => theme.colors.primary700};
  border-radius: ${({ theme }) => theme.borderRadius};
  z-index: 1;
  pointer-events: none;
`;
var DropZoneWithOverlay = ({ children }) => {
  const { isDragging } = useUploadDropZone();
  return (0, import_jsx_runtime16.jsxs)(R, {
    position: "relative",
    children: [
      isDragging && (0, import_jsx_runtime16.jsx)(DropZoneOverlay, {}),
      children
    ]
  });
};
var DropFilesMessageImpl = gt(R)`
  position: fixed;
  bottom: ${({ theme }) => theme.spaces[8]};
  left: 50%;
  transform: translateX(calc(-50% + ${({ $leftContentWidth }) => $leftContentWidth / 2}px));
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: ${({ theme }) => theme.spaces[2]};
  background: ${({ theme }) => theme.colors.primary600};
  padding: ${({ theme }) => theme.spaces[4]} ${({ theme }) => theme.spaces[6]};
  border-radius: ${({ theme }) => theme.borderRadius};
  z-index: 2;
`;
var DropFilesMessage = ({ uploadDropZoneRef, folderName }) => {
  const { formatMessage } = useIntl();
  const { isDragging } = useUploadDropZone();
  const [leftContentWidth, setLeftContentWidth] = React18.useState(0);
  React18.useEffect(() => {
    if (!(uploadDropZoneRef == null ? void 0 : uploadDropZoneRef.current)) return;
    const updateRect = () => {
      var _a;
      const rect = (_a = uploadDropZoneRef.current) == null ? void 0 : _a.getBoundingClientRect();
      if (rect) {
        setLeftContentWidth((prev) => prev !== rect.left ? rect.left : prev);
      }
    };
    updateRect();
    const resizeObserver = new ResizeObserver(updateRect);
    resizeObserver.observe(uploadDropZoneRef.current);
    return () => resizeObserver.disconnect();
  }, [
    uploadDropZoneRef
  ]);
  if (!isDragging) return null;
  return (0, import_jsx_runtime16.jsxs)(DropFilesMessageImpl, {
    $leftContentWidth: leftContentWidth,
    children: [
      (0, import_jsx_runtime16.jsx)(I, {
        textColor: "neutral0",
        children: formatMessage({
          id: getTranslationKey("dropzone.upload.message"),
          defaultMessage: "Drop here to upload to"
        })
      }),
      (0, import_jsx_runtime16.jsxs)(T, {
        gap: 2,
        alignItems: "center",
        children: [
          (0, import_jsx_runtime16.jsx)(K5, {
            width: 20,
            height: 20,
            fill: "neutral0"
          }),
          (0, import_jsx_runtime16.jsx)(I, {
            textColor: "neutral0",
            fontWeight: "semiBold",
            children: folderName
          })
        ]
      })
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/components/ImportFromUrlDialog.mjs
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var React19 = __toESM(require_react(), 1);
var ImportFromUrlDialog = ({ open, onClose, onUpload }) => {
  const { formatMessage } = useIntl();
  const [urls, setUrls] = React19.useState("");
  const [error, setError] = React19.useState(null);
  const handleClose = () => {
    setUrls("");
    setError(null);
    onClose();
  };
  const handleSubmit = async (e) => {
    e.preventDefault();
    const { urls: validUrls, error: validationError } = validateUrls(urls);
    if (validationError) {
      setError(validationError);
      return;
    }
    setError(null);
    handleClose();
    await onUpload(validUrls);
  };
  return (0, import_jsx_runtime17.jsx)(n1.Root, {
    open,
    onOpenChange: (isOpen) => !isOpen && handleClose(),
    children: (0, import_jsx_runtime17.jsx)(n1.Content, {
      children: (0, import_jsx_runtime17.jsxs)("form", {
        onSubmit: handleSubmit,
        children: [
          (0, import_jsx_runtime17.jsx)(n1.Header, {
            children: (0, import_jsx_runtime17.jsx)(n1.Title, {
              children: formatMessage({
                id: getTranslationKey("modal.url.title"),
                defaultMessage: "Import from URL"
              })
            })
          }),
          (0, import_jsx_runtime17.jsx)(n1.Body, {
            children: (0, import_jsx_runtime17.jsxs)(Vm.Root, {
              error: error || void 0,
              hint: formatMessage({
                id: getTranslationKey("input.url.description"),
                defaultMessage: "Separate your URL links by a carriage return."
              }),
              children: [
                (0, import_jsx_runtime17.jsx)(Vm.Label, {
                  children: formatMessage({
                    id: getTranslationKey("input.url.label"),
                    defaultMessage: "URL(s)"
                  })
                }),
                (0, import_jsx_runtime17.jsx)(O1, {
                  name: "urls",
                  minHeight: "unset",
                  rows: Math.min(urls.split("\n").length, 7),
                  maxHeight: "10.5rem",
                  placeholder: formatMessage({
                    id: getTranslationKey("input.url.placeholder"),
                    defaultMessage: "Empty"
                  }),
                  value: urls,
                  onChange: (e) => {
                    setUrls(e.target.value);
                    setError(null);
                  }
                }),
                (0, import_jsx_runtime17.jsx)(Vm.Hint, {}),
                (0, import_jsx_runtime17.jsx)(Vm.Error, {})
              ]
            })
          }),
          (0, import_jsx_runtime17.jsxs)(n1.Footer, {
            children: [
              (0, import_jsx_runtime17.jsx)(zn, {
                variant: "tertiary",
                onClick: handleClose,
                children: formatMessage({
                  id: "app.components.Button.cancel",
                  defaultMessage: "Cancel"
                })
              }),
              (0, import_jsx_runtime17.jsx)(zn, {
                type: "submit",
                children: formatMessage({
                  id: getTranslationKey("modal.url.upload"),
                  defaultMessage: "Upload"
                })
              })
            ]
          })
        ]
      })
    })
  });
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useFolderInfo.mjs
var useFolderInfo = (currentFolderId) => {
  var _a, _b;
  const { formatMessage } = useIntl();
  const { data: currentFolder, isLoading } = useGetFolderQuery({
    id: currentFolderId
  }, {
    skip: currentFolderId === null
  });
  const { data: rootAssetsData, isLoading: isRootAssetsLoading } = useGetAssetsQuery({
    folder: null,
    pageSize: 1
  }, {
    skip: currentFolderId !== null
  });
  const homeLabel = formatMessage({
    id: getTranslationKey("plugin.home"),
    defaultMessage: "Home"
  });
  if (currentFolderId === null) {
    if (isRootAssetsLoading) {
      return {
        title: homeLabel,
        itemCount: 0
      };
    }
    return {
      title: homeLabel,
      itemCount: ((_a = rootAssetsData == null ? void 0 : rootAssetsData.pagination) == null ? void 0 : _a.total) ?? 0
    };
  }
  if (isLoading) {
    return {
      title: homeLabel,
      itemCount: 0
    };
  }
  return {
    title: (currentFolder == null ? void 0 : currentFolder.name) ?? homeLabel,
    itemCount: ((_b = currentFolder == null ? void 0 : currentFolder.files) == null ? void 0 : _b.count) ?? 0
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/hooks/useInfiniteAssets.mjs
var import_react6 = __toESM(require_react(), 1);
var PAGE_SIZE = 20;
var useInfiniteAssets = ({ folder = null, sort } = {}) => {
  const [page, setPage] = (0, import_react6.useState)(1);
  const lastResultsRef = (0, import_react6.useRef)([]);
  const isMountRef = (0, import_react6.useRef)(true);
  const { currentData: data, isLoading, isFetching, error } = useGetAssetsQuery({
    folder,
    page,
    pageSize: PAGE_SIZE,
    sort
  });
  const pagination = data == null ? void 0 : data.pagination;
  const assets = (0, import_react6.useMemo)(() => {
    if (!data) {
      return lastResultsRef.current;
    }
    const currentPageResults = data.results;
    if (page === 1) {
      lastResultsRef.current = currentPageResults;
    } else {
      const expectedPrior = (page - 1) * PAGE_SIZE;
      if (lastResultsRef.current.length < expectedPrior - PAGE_SIZE) {
        return lastResultsRef.current;
      }
      if (lastResultsRef.current.length < page * PAGE_SIZE) {
        lastResultsRef.current = [
          ...lastResultsRef.current,
          ...currentPageResults
        ];
      }
    }
    return lastResultsRef.current;
  }, [
    data,
    page
  ]);
  (0, import_react6.useEffect)(() => {
    if (isMountRef.current) {
      isMountRef.current = false;
      return;
    }
    setPage(1);
    lastResultsRef.current = [];
  }, [
    folder,
    sort
  ]);
  const hasNextPage = pagination ? page < pagination.pageCount : false;
  const isFetchingMore = isFetching && page > 1;
  const fetchNextPage = (0, import_react6.useCallback)(() => {
    setPage((prev) => prev + 1);
  }, []);
  return {
    assets,
    pagination,
    isLoading,
    isFetchingMore,
    hasNextPage,
    fetchNextPage,
    error
  };
};

// node_modules/@strapi/upload/dist/admin/future/pages/Assets/AssetsPage.mjs
var INTERSECTION_OPTIONS = {
  threshold: 0.1
};
var AssetsView = ({ view, folderId, onAssetItemClick }) => {
  const { formatMessage } = useIntl();
  const { assets, isLoading: isLoadingAssets, isFetchingMore, hasNextPage, fetchNextPage, error } = useInfiniteAssets({
    folder: folderId
  });
  const { data: folders = [], isLoading: isLoadingFolders } = useGetFoldersQuery({
    parentId: folderId
  });
  const isGridView = view === viewOptions.GRID;
  const isLoading = isLoadingAssets || isLoadingFolders;
  const loadMoreRef = useElementOnScreen((0, import_react7.useCallback)((isVisible) => {
    if (isVisible && hasNextPage && !isFetchingMore) {
      fetchNextPage();
    }
  }, [
    hasNextPage,
    isFetchingMore,
    fetchNextPage
  ]), INTERSECTION_OPTIONS);
  if (isLoading) {
    return (0, import_jsx_runtime18.jsx)(T, {
      justifyContent: "center",
      padding: 8,
      children: (0, import_jsx_runtime18.jsx)(Fn, {
        children: formatMessage({
          id: "app.loading",
          defaultMessage: "Loading..."
        })
      })
    });
  }
  if (error) {
    return (0, import_jsx_runtime18.jsx)(R, {
      padding: 8,
      children: (0, import_jsx_runtime18.jsx)(I, {
        textColor: "danger600",
        children: formatMessage({
          id: getTranslationKey("list.assets.error"),
          defaultMessage: "An error occurred while fetching assets."
        })
      })
    });
  }
  if (folders.length === 0 && assets.length === 0) {
    return (0, import_jsx_runtime18.jsx)(R, {
      padding: 8,
      children: (0, import_jsx_runtime18.jsx)(I, {
        textColor: "neutral600",
        children: formatMessage({
          id: "app.components.EmptyStateLayout.content-document",
          defaultMessage: "No content found"
        })
      })
    });
  }
  return (0, import_jsx_runtime18.jsxs)(import_jsx_runtime18.Fragment, {
    children: [
      isGridView ? (0, import_jsx_runtime18.jsx)(AssetsGrid, {
        folders,
        assets,
        onAssetItemClick
      }) : (0, import_jsx_runtime18.jsx)(AssetsTable, {
        assets,
        folders,
        onAssetItemClick
      }),
      (0, import_jsx_runtime18.jsx)("div", {
        ref: loadMoreRef,
        style: {
          height: 1
        }
      }),
      isFetchingMore && (0, import_jsx_runtime18.jsx)(T, {
        justifyContent: "center",
        padding: 4,
        children: (0, import_jsx_runtime18.jsx)(Fn, {
          children: formatMessage({
            id: getTranslationKey("list.assets.loading-more"),
            defaultMessage: "Loading more assets..."
          })
        })
      })
    ]
  });
};
var StyledToggleGroup = gt(Root2)`
  display: flex;
  border: 1px solid ${({ theme }) => theme.colors.neutral200};
  border-radius: ${({ theme }) => theme.borderRadius};
  overflow: hidden;
`;
var StyledToggleItem = gt(Item2)`
  display: flex;
  align-items: center;
  gap: ${({ theme }) => theme.spaces[2]};
  padding: ${({ theme }) => `${theme.spaces[2]} ${theme.spaces[4]}`};
  border: none;
  background: ${({ theme }) => theme.colors.neutral0};
  color: ${({ theme }) => theme.colors.neutral800};
  cursor: pointer;
  font-size: ${({ theme }) => theme.fontSizes[1]};
  font-weight: ${({ theme }) => theme.fontWeights.semiBold};

  &:hover {
    background: ${({ theme }) => theme.colors.primary100};
  }

  &[data-state='on'] {
    background: ${({ theme }) => theme.colors.neutral150};
  }

  svg {
    width: 1.6rem;
    height: 1.6rem;
  }
`;
var HeaderWrapper = gt(R)`
  [data-strapi-header] {
    background: ${({ theme }) => theme.colors.neutral0};

    h1 {
      font-size: 1.8rem;
    }
  }
`;
var AssetsPage = () => {
  const { formatMessage } = useIntl();
  const { openDetails } = useAssetDetailsParam();
  const { currentFolderId } = useFolderNavigation();
  const { title, itemCount } = useFolderInfo(currentFolderId);
  const itemCountLabel = formatMessage({
    id: getTranslationKey("header.content.item-count"),
    defaultMessage: "{count, plural, =1 {# item} other {# items}}"
  }, {
    count: itemCount
  });
  const [isCreateFolderDialogOpen, setIsCreateFolderDialogOpen] = (0, import_react7.useState)(false);
  const [view, setView] = usePersistentState(localStorageKeys.view, viewOptions.GRID);
  const isGridView = view === viewOptions.GRID;
  const [isUrlDialogOpen, setIsUrlDialogOpen] = (0, import_react7.useState)(false);
  const fileInputRef = (0, import_react7.useRef)(null);
  const uploadDropZoneRef = (0, import_react7.useRef)(null);
  const [uploadFilesStream] = useUploadFilesStreamMutation();
  const [uploadFromUrls] = useUploadFromUrlsMutation();
  const uploadFilesToFolder = async (files, folderId) => {
    if (files.length === 0) return;
    const formData = new FormData();
    const fileInfoArray = [];
    files.forEach((file) => {
      formData.append("files", file);
      fileInfoArray.push({
        name: file.name,
        caption: null,
        alternativeText: null,
        folder: folderId
      });
    });
    formData.append("fileInfo", JSON.stringify(fileInfoArray));
    try {
      await uploadFilesStream({
        formData,
        totalFiles: files.length
      }).unwrap();
    } catch (error) {
    }
  };
  const handleFileSelect = () => {
    var _a;
    (_a = fileInputRef.current) == null ? void 0 : _a.click();
  };
  const handleFileChange = async (e) => {
    const files = e.target.files;
    if (files && files.length > 0) {
      await uploadFilesToFolder(Array.from(files), currentFolderId);
    }
    e.target.value = "";
  };
  const handleDrop = async (files) => {
    await uploadFilesToFolder(files, currentFolderId);
  };
  const handleUrlUpload = async (urls) => {
    try {
      await uploadFromUrls({
        urls,
        folderId: currentFolderId
      }).unwrap();
    } catch (error) {
    }
  };
  return (0, import_jsx_runtime18.jsxs)(import_jsx_runtime18.Fragment, {
    children: [
      (0, import_jsx_runtime18.jsx)(UploadDropZoneProvider, {
        onDrop: handleDrop,
        children: (0, import_jsx_runtime18.jsx)(R, {
          ref: uploadDropZoneRef,
          children: (0, import_jsx_runtime18.jsxs)(Layouts.Root, {
            minHeight: "100vh",
            background: "neutral0",
            children: [
              (0, import_jsx_runtime18.jsx)(at, {
                children: (0, import_jsx_runtime18.jsx)("input", {
                  type: "file",
                  ref: fileInputRef,
                  onChange: handleFileChange,
                  multiple: true
                })
              }),
              (0, import_jsx_runtime18.jsx)(HeaderWrapper, {
                children: (0, import_jsx_runtime18.jsx)(Layouts.Header, {
                  title: `${title} (${itemCountLabel})`,
                  primaryAction: (0, import_jsx_runtime18.jsxs)(hd, {
                    popoverPlacement: "bottom-end",
                    variant: "default",
                    endIcon: (0, import_jsx_runtime18.jsx)(c5, {}),
                    label: formatMessage({
                      id: getTranslationKey("new"),
                      defaultMessage: "New"
                    }),
                    children: [
                      (0, import_jsx_runtime18.jsx)(Tm, {
                        onSelect: () => setIsCreateFolderDialogOpen(true),
                        startIcon: (0, import_jsx_runtime18.jsx)(K5, {}),
                        children: formatMessage({
                          id: getTranslationKey("folder.create.title"),
                          defaultMessage: "New folder"
                        })
                      }),
                      (0, import_jsx_runtime18.jsx)(Tm, {
                        onSelect: handleFileSelect,
                        startIcon: (0, import_jsx_runtime18.jsx)(N5, {}),
                        children: formatMessage({
                          id: getTranslationKey("import-files"),
                          defaultMessage: "Import files"
                        })
                      }),
                      (0, import_jsx_runtime18.jsx)(Tm, {
                        onSelect: () => setIsUrlDialogOpen(true),
                        startIcon: (0, import_jsx_runtime18.jsx)(H3, {}),
                        children: formatMessage({
                          id: getTranslationKey("import-from-url"),
                          defaultMessage: "Import from URL"
                        })
                      })
                    ]
                  }),
                  subtitle: (0, import_jsx_runtime18.jsxs)(T, {
                    justifyContent: "space-between",
                    alignItems: "center",
                    gap: 4,
                    width: "100%",
                    children: [
                      (0, import_jsx_runtime18.jsx)(T, {
                        gap: 4,
                        alignItems: "center",
                        children: "TODO: Filters and search"
                      }),
                      (0, import_jsx_runtime18.jsxs)(T, {
                        gap: 4,
                        alignItems: "center",
                        children: [
                          (0, import_jsx_runtime18.jsx)(R, {
                            children: "TODO: Sort"
                          }),
                          (0, import_jsx_runtime18.jsxs)(StyledToggleGroup, {
                            type: "single",
                            value: isGridView ? "grid" : "table",
                            onValueChange: (value) => value && setView(value === "grid" ? viewOptions.GRID : viewOptions.TABLE),
                            "aria-label": formatMessage({
                              id: getTranslationKey("view.switch.label"),
                              defaultMessage: "View options"
                            }),
                            children: [
                              (0, import_jsx_runtime18.jsxs)(StyledToggleItem, {
                                value: "table",
                                "aria-label": formatMessage({
                                  id: getTranslationKey("view.table"),
                                  defaultMessage: "Table view"
                                }),
                                children: [
                                  (0, import_jsx_runtime18.jsx)(A3, {}),
                                  formatMessage({
                                    id: getTranslationKey("view.table"),
                                    defaultMessage: "Table view"
                                  })
                                ]
                              }),
                              (0, import_jsx_runtime18.jsxs)(StyledToggleItem, {
                                value: "grid",
                                "aria-label": formatMessage({
                                  id: getTranslationKey("view.grid"),
                                  defaultMessage: "Grid view"
                                }),
                                children: [
                                  (0, import_jsx_runtime18.jsx)(k5, {}),
                                  formatMessage({
                                    id: getTranslationKey("view.grid"),
                                    defaultMessage: "Grid view"
                                  })
                                ]
                              })
                            ]
                          })
                        ]
                      })
                    ]
                  })
                })
              }),
              (0, import_jsx_runtime18.jsx)(Layouts.Content, {
                children: (0, import_jsx_runtime18.jsxs)(DropZoneWithOverlay, {
                  children: [
                    (0, import_jsx_runtime18.jsx)(DropFilesMessage, {
                      uploadDropZoneRef,
                      folderName: title
                    }),
                    (0, import_jsx_runtime18.jsx)(AssetsView, {
                      view,
                      folderId: currentFolderId,
                      onAssetItemClick: openDetails
                    })
                  ]
                })
              })
            ]
          })
        })
      }),
      (0, import_jsx_runtime18.jsx)(CreateFolderDialog, {
        open: isCreateFolderDialogOpen,
        folderName: title,
        parentFolderId: currentFolderId,
        onClose: () => setIsCreateFolderDialogOpen(false)
      }),
      (0, import_jsx_runtime18.jsx)(ImportFromUrlDialog, {
        open: isUrlDialogOpen,
        onClose: () => setIsUrlDialogOpen(false),
        onUpload: handleUrlUpload
      }),
      (0, import_jsx_runtime18.jsx)(AssetDetailsDrawer, {})
    ]
  });
};

// node_modules/@strapi/upload/dist/admin/future/App.mjs
var UnstableMediaLibrary = () => {
  const { formatMessage } = useIntl();
  const title = formatMessage({
    id: getTranslationKey("plugin.name"),
    defaultMessage: "Media Library"
  });
  return (0, import_jsx_runtime19.jsxs)(Page.Main, {
    children: [
      (0, import_jsx_runtime19.jsx)(Page.Title, {
        children: title
      }),
      (0, import_jsx_runtime19.jsx)(Routes, {
        children: (0, import_jsx_runtime19.jsx)(Route, {
          index: true,
          element: (0, import_jsx_runtime19.jsx)(AssetsPage, {})
        })
      })
    ]
  });
};
export {
  UnstableMediaLibrary
};
//# sourceMappingURL=App-TYD2W7XP.js.map
